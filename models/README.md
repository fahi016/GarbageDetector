Place your trained waste-detection weights here as:

  waste_detector.pt     (Ultralytics YOLO)
  or another format loaded by existing_model_example.py

The simulation default is the mock color detector so the demo runs
before your model file is copied in.
